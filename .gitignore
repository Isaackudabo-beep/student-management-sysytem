node_modules/
web/.next/
.env
.env.local
*.log
.DS_Store
coverage/
.turbo/
*.zip
.prisma/

# Keep api/dist in git so Render can boot even if build is misconfigured.
# web build output stays ignored above.
/web/dist/
